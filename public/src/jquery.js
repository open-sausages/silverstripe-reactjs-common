/**
 * @file Alias for jQuery so it can be imported like other dependencies.
 * Can be removed when Framework has a bundle that exposes jQuery.
 */

var jquery = jQuery !== void 0 ? jQuery : {};

module.exports = jquery;
